c  <% if obj.errors.any? %>
    <div class="row">
      <div class="col-sm-8 offset-sm-2">
        <div class="card bg-danger">
          <div class="card-header">
              <h3><%= pluralize(obj.errors.count, "error") %> prohibited this post from being saved:</h3>
          </div>
          <div class="card-body">
              <ul>
                <% obj.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              <ul>
          </div>
        </div>
    </div>
  </div>
<% end %>